# Enable the key matrix and general features
SRC = keymap.c

# Enable the encoder feature
ENCODER_ENABLE = yes

# Enable mapping of encoder actions via the keymap.c (recommended)
ENCODER_MAP_ENABLE = yes

# MCU and bootloader settings (common for Pro Micro/atmega32u4)
MCU = atmega32u4
BOOTLOADER = caterina